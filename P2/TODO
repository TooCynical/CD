-parser: 
    -Sanitize negative values from input (currently cast to unsigned ints).
-Sequence: (done) implement set_order(...)
-Floorplan: 
    -(done) Implement verify 
    -(done) Implement print_floorplan_with_dimensions.
-Drawer:
    -(done)  make it use .floorplan-files only
-command line options:
    - (done) Floorplan with / without dimensions.
-Solver:
    - (scrapped) Make a parallel solver.
- Testing!
